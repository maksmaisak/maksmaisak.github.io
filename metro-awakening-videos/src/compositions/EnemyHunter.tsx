import { clipComposition } from "../clipComposition";

export const spec = clipComposition({
  id: "enemy-hunter",
  src: "enemy_hunter.mp4",
  durationSeconds: 12,
  placeholder:
    "Hunters using hit-and-run tactics: hiding with line of sight, " +
    "dodging toward next destination, pair attacks from different angles. 10-15s.",
});
